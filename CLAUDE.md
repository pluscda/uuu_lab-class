# CLAUDE.md

Full-stack CMS generated from the SQL Server schemas in `database/*.sql`
(auth, admin, course, promotion).

## Structure

`src/CMS.slnx` solution (.NET SDK 10 format) · `src/CMS.API` .NET 9 Web API +
Dapper · `src/CMS.API.Tests` xUnit + Moq · `src/CMS.NG` Angular 20 + PrimeNG 20

## Reference Files (details live there, not here)

- `spec/code-gen.convention.md` — **all** coding conventions: backend, frontend,
  date handling, special types, cross-cutting (RowAudit + exception handling).
  **Read before generating or modifying feature code.**
- `spec/status.md` — completed modules + entity gotchas, auth implementation
  details, lookup endpoints, sidebar groups, test counts, remaining work.
  **Read before planning a module; update after finishing one.**
- `spec/{sub-system}/{Table}.md` — per-entity feature specs generated by `/crud`.
- `spec/ui-sample-*.png` — UI style reference. Visual style only; never copy content.

## Commands

- API: `cd src/CMS.API && dotnet run` → http://127.0.0.1:5000 (Swagger `/swagger`)
- Backend tests: `cd src && dotnet test CMS.slnx`
- Frontend: `cd src/CMS.NG && npm start` → http://localhost:4200
- Frontend tests / build: `ng test --watch=false --browsers=ChromeHeadless` / `ng build`

## Auth Essentials (implementation details in `spec/status.md`)

- Every endpoint requires a JWT Bearer token except `POST /api/auth/login`
  (global FallbackPolicy). Signing/validation key = SysConfig
  `appConfig.symmetricSecurityKey`, read from DB at runtime — never appsettings.
- Frontend: session-storage profile (`auth-profile`), `authInterceptor` +
  `authGuard`; `系統管理 Admin` sidebar group only for role `Admin`.
- Dev login: `miles@uuu.com.tw` / `CMS4fun#` (hash = SHA-256 UTF-8 uppercase hex).

## Cross-Cutting Checklist (every feature; rules in `spec/code-gen.convention.md`)

- Repository Insert/Update/Delete → RowAudit via `IRowAuditWriter`, on the same
  connection/transaction as the change.
- Detail + form pages → `<app-row-audit-badge [tableName] [pkid]>` in the
  toolbar `#start` slot.
- No per-controller try/catch — global `ExceptionHandlingMiddleware` returns
  safe 500s; Angular interceptor toasts 5xx, 401 → logout.

## Hard Constraints (never change)

- API binds `127.0.0.1:5000` — never `localhost` (Docker Desktop holds the IPv6 port).
- Only .NET SDK 10 installed; projects keep `net9.0` + `<RollForward>LatestMajor</RollForward>`.
- PrimeNG **20** — never upgrade to 21.
- Dapper only — no Entity Framework.
- DB: local `SQLEXPRESS`, database `CMS`, `Trusted_Connection=True`;
  connection string in `src/CMS.API/appsettings.json`.
